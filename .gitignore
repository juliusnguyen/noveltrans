# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/

# Tooling
.pytest_cache/
.ruff_cache/
.coverage

# IDE
.idea/
.vscode/

# App data (never commit downloaded novels) — anchored to the repo root:
# on case-insensitive filesystems an unanchored rule also matches src/noveltrans/
/NovelTrans/
*.db
*.db-wal
*.db-shm

# Windows packaging: bundled ffmpeg/ffprobe binaries, fetched locally before `.\make.ps1
# app` — see README "Đóng gói thành app Windows". Not committed (license + size).
/packaging/ffmpeg/

# OS
.DS_Store

# large source logo (downscaled copy lives in assets/vinyl_label.png)
/design/fox_logo.png
