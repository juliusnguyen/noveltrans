# 073 — APP LOGO UPDATE

Update the application logo assets:

- **App logo** (application/window/dock icon): use `design/logo-icon.png`
- **App logo bar** (menu bar / tray icon): use `design/bar-icon.png`

Regenerate all derived icon sizes/formats from these sources and replace the
existing icons wherever they are referenced (build config, packaging, runtime
assets).
