# 079 — BQG5 Scraper

Add scraper support for this site: https://m.bqg5.com/4_4217/

Note from the user: for some reason, the site is only viewable on mobile — it can't
be viewed from this desktop. So the implementation likely needs to account for
mobile-only access (e.g. a mobile user agent / mobile viewport, or the `m.` mobile
host), and any anti-bot or desktop-blocking behaviour the site applies.

Follow the existing scraper conventions in `src/noveltrans/scrapers/` (see
`bookqq.py`, `timotxt.py`, `twkan.py` for recent examples) — including registration,
tests, and whatever browser/Cloudflare handling the site requires.
