# 014 — TTS Parallel Workers

Make TTS use multiple workers so synthesis can run in parallel.

- Default worker count is **1** (current behavior preserved).
- The worker count is configurable in **Settings**.
- Increasing workers should parallelize TTS synthesis across chapters/segments to speed up long jobs.
