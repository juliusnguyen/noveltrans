# 015 — 69shuba Source Support — History

## [FEATURE] 015-69SHUBA-SOURCE — 2026-07-16
Created feature scaffolding and branch `changes/015-69SHUBA-SOURCE`.
Prompt: `015.00-PROMPT.md`.

## [RESEARCH] Cloudflare challenge blocks the plain-requests approach — 2026-07-16
Probed the target site before planning. `https://www.69shuba.com/book/59024/` returns
**HTTP 403** with `cf-mitigated: challenge` and a CSP referencing
`challenges.cloudflare.com` — a Cloudflare **managed challenge**, not the trivial JS
token that `ixdzs` solves inline.

Confirmed the block is site-wide, not host-specific — all 403:
- `www.69shuba.com`, `69shuba.com`, `www.69shu.com`, `www.69shuba.cx`
- (`m.69shuba.com` does not resolve)

Consequence: a `requests`-based adapter in the shape of `xbanxia`/`ixdzs` cannot work
here — every fetch returns the interstitial rather than the book page. The real page
HTML remains **unseen**, so no selectors can be written until a browser session
captures it.

**Decision (user):** solve the challenge with a **headless browser (Playwright)**.
Alternatives raised and declined: pasting a `cf_clearance` cookie from the user's own
browser (the `medoctruyen` precedent — fragile, UA/IP-bound, expires mid-run);
TLS-fingerprint impersonation via `curl_cffi` (declined — its purpose is defeating bot
detection); dropping 69shuba for a plain-HTML source.

Materially lowers the cost of that choice: **Playwright is already a dependency** —
`pyproject.toml` carries `playwright>=1.40` under the `discord` extra, and
`src/noveltrans/discord_unlock.py` already establishes the house pattern (lazy import
with install instructions, `launch_persistent_context` on a dedicated profile dir,
real-Chrome-then-bundled-Chromium channel fallback, `--disable-blink-features=
AutomationControlled`). The new adapter should follow that module rather than invent
its own browser handling.

## [PLAN] 015.01-INITIAL-PLAN — 2026-07-16
Generated plan: `015.01-INITIAL-PLAN.md`

## [STEP 1] Extract shared browser module — 2026-07-16
Plan §2 Step 1. Pure refactor of working code (feature 004), no behaviour change.

**Added `src/noveltrans/browser.py`** — the launch machinery formerly private to
`discord_unlock.py`, now shared with the upcoming Cloudflare solver:
- `BrowserUnavailableError` — generic, English. This module is infrastructure; the
  app speaks Vietnamese, so callers catch and re-raise their own strings.
- `require_playwright()` — lazy import + install hint (`noveltrans[browser]`).
- `launch_persistent_context(sync_playwright, profile_dir, *, headless)` — real Chrome
  then bundled Chromium, `--disable-blink-features=AutomationControlled`,
  `ignore_default_args=["--enable-automation"]`. Stops Playwright before raising so no
  process leaks. Takes `sync_playwright` as an argument so tests can inject a fake;
  takes `profile_dir` as an argument so each caller keeps its own profile.
- `close(context, playwright)` — never-raises teardown.

Rationale for extracting rather than duplicating: these are anti-bot-detection args
whose entire value is being correct and consistent. Two copies drifting apart would
mean one caller silently losing the disguise.

**Rewired `src/noveltrans/discord_unlock.py`** — `_require_playwright()` and
`_launch_context()` are now thin shims that delegate and translate
`BrowserUnavailableError` back into the Vietnamese `DiscordUnlockError`. Removed the
moved `_BROWSER_CHANNELS`, `_LAUNCH_ARGS` and `_close`; `_close` is now imported from
`browser`. Discord-specific DOM selectors (`_PICKER_OPTION_SEL` etc.) stayed put.

**`tests/test_discord_unlock.py` needed no changes** — the plan flagged it as likely
to break since it imports `_launch_context` and `_PICKER_OPTION_SEL` directly. Keeping
`_launch_context` as a shim with the same signature preserved both, so the existing
tests still exercise the new code path through the shim (incl. the Chrome-preferred,
bundled-fallback and no-browser-at-all cases, and the `fake.stopped` no-leak assertion
which now covers `browser.py`'s stop-before-raise).

Verified: `256 passed, 5 deselected`; `ruff check src tests` clean. Green in isolation
before proceeding, per the plan's instruction.

## [STEP 2] Capture fixtures — R1 FAILED, hybrid design is dead — 2026-07-16
Plan §2 Step 2 (the blocking step). Added `scripts/capture_69shuba.py`. Installed
`playwright>=1.40` into the venv (`uv pip install`); real Chrome is present at
`/Applications/Google Chrome.app`, so the `channel="chrome"` path is used and no
bundled-Chromium download was needed.

### R1 failed — and worse than the plan predicted
The browser cleared the challenge and reached the real page. Replaying its
User-Agent + cookies over plain `requests` still returned **403 `cf-mitigated:
challenge`**.

The cause is not a cookie that failed to transfer — **no `cf_clearance` cookie was
issued at all**. The only cookie set was `zh_choose` (a language preference). Real
Chrome was passed *silently on fingerprint*, without an interstitial and without a
token. So there is nothing to replay: Cloudflare here gates on TLS/browser
fingerprint, which `requests` cannot reproduce at any header-copying effort. The
hybrid is not "fragile", it is **impossible**.

Also confirmed the plan's UA point: the real Chrome UA is `Chrome/150.0.0.0`, against
the `Chrome/126` hardcoded in `base.py:14`.

**Consequence — the architecture flips to always-Playwright** (`_get_html` drives the
browser), exactly the fallback plan §1 named. Because `_get_html` was designed as the
only seam, parsing/tests/fixtures survive the pivot. Knock-on effects:
- **Plan §2 Step 4 (`base.py` changes) is now largely unnecessary.** 69shuba never
  touches `HttpClient`, so the UA override (4a), the `requests`-side challenge check
  (4b) and the forced GBK encoding (4c) have no user. Chrome owns decoding, so the
  encoding question evaporates. Keep only `is_challenge_html(markup)` for spotting an
  interstitial in `page.content()`; drop `is_challenge_response(response)`.
- **Plan §3's `load_fixture_bytes` is unnecessary.** Fixtures are `page.content()` —
  already-decoded UTF-8, exactly what the adapter receives — so the existing
  `load_fixture` is correct and `responses` isn't involved at all. Tests should feed
  saved markup to pure parsing helpers, which keeps them browser-free. This argues for
  splitting fetch from parse in the adapter.
- **New concern the plan didn't cost:** 199 chapters × one navigation each. Needs one
  persistent browser for the batch plus resource-blocking (images/fonts/media), not a
  launch per chapter. Reinforces plan §5 R4: do not parallelize this site.

### Fixtures captured (`tests/fixtures/69shuba/`)
69shuba splits across three pages, where other sites use one — the plan assumed one:
| fixture | url | what |
|---|---|---|
| `novel.html` | `/book/59024.htm` | info page — title/author/cover/description |
| `toc.html` | `/book/59024/` | chapter list (199 links) |
| `chapter.html` | `/txt/59024/38369377` | one chapter body |

A first attempt captured a login page as `chapter.html`: the link matcher tested
`"/article/" in href` and hit `/modules/article/bookcase.php` ("add to bookcase"),
which bounces to `用户登录`. Fixed to require `/txt/\d+/\d+`; bad fixture deleted and
re-captured.

### Plan §6 checklist — ANSWERED
**Encoding (1-3):** moot. Playwright hands over decoded text; Chrome does the decoding.
Drop Step 4c. (The site's raw charset stays unknown — `requests` never saw a real page.)

**URLs (4-7):**
- Info page is `/book/<id>.htm`; `/book/<id>/` is the **TOC**, a separate page. The
  adapter must fetch both, and `url_patterns` must accept either.
- Chapters are `/txt/<book_id>/<chapter_id>`, ids sequential (`下一章` → `…38369378`).
- Only `www.69shuba.com` verified. `.cx`/`69shu.com` also 403 but were never seen past
  the challenge — do not assume the same markup.

**Structure (8-14):**
- Metadata: the info page carries **full OpenGraph tags** — `og:novel:book_name`,
  `og:novel:author`, `og:description`, `og:image`, `og:novel:latest_chapter_name`.
  Use them (the `ixdzs` approach), not CSS. Note `og:description` embeds literal
  `<br />` needing cleanup.
- TOC: `#catalog ul li a[href]`.
- Content: `.txtnav` — **`<br>`-separated text nodes, zero `<p>`** → the `xbanxia`
  extraction shape, not `ixdzs`.
- Chrome inside `.txtnav` to decompose: `h1.hide720`, `div.txtinfo.hide720`,
  `div#txtright`, `div.contentadv`, `div.bottom-ad`.
- **TOC does NOT paginate.** 199 links, and `og:novel:latest_chapter_name` is
  `199.第199章 …` — counts match, so the whole TOC is on one page. No pagination loop
  (plan §2 Step 6's `medoctruyen` fallback is not needed).
- **Title-dup strip needs care.** The first body line duplicates the title, but
  *not exactly*: TOC/h1 gives `1.第1章 …` while the body line is `第1章 …`. The site's
  own titles are inconsistent — ch.1 is `1.第1章 …` but ch.2 is plain `第2章 …`.
  `xbanxia`'s `lines[0] == ref.title` would silently fail to strip. Needs a tolerant
  match (e.g. `ref.title.endswith(line)` / index-prefix normalisation).
- No obfuscation seen. Chapter 1 is ~2,102 chars against `40.75万字`/199 ≈ 2,048
  average — consistent with full content, no preview gate or paywall.

**Cloudflare (15-20):**
- (15) **No** — clearance does not survive outside the browser; none is issued.
- (16) Challenge = HTTP 403 + `cf-mitigated: challenge`, ~5.6KB UTF-8 interstitial.
- (17) 403 every time across 4 hosts; no 503/200 variant seen.
- (18) N/A — no cookie, so no lifetime. Re-solve concerns are moot; the browser session
  is the clearance.
- (19) **Passive** for real Chrome — cleared with no interaction. Headed still
  recommended, but no checkbox was required in this run.
- (20) Only `zh_choose`; no Cloudflare cookies at all.

Verified: `256 passed`; `ruff check` clean (no src changes this step).

## [PLAN] 015.02-REVISED-ARCHITECTURE — 2026-07-16
Generated plan: `015.02-REVISED-ARCHITECTURE.md`. Supersedes 015.01's architecture
after Step 2's R1 failure. Always-Playwright; implementation resumes at Step 3.

Notable deltas beyond the pivot itself:
- **`HttpClient` is untouched** — all three of 015.01's `base.py` changes dropped, so
  the four shipping adapters carry zero regression surface. The only `base.py` change
  is a new no-op `SiteAdapter.close()` for browser teardown, which 015.01 never
  anticipated needing.
- **`cloudflare.py`, `ChallengeError`, `load_fixture_bytes`, the pagination loop** —
  all dropped as having no caller under the new design.
- Caught a regex bug in 015.01's proposed `url_patterns`: `r"69shuba?\."` parses as
  `69shub` + optional `a`, so it matches `69shub.com` and misses `69shu.com`.
- Flagged an out-of-scope staleness bug: `base.py:14` hardcodes `Chrome/126` while
  real Chrome is `Chrome/150` — affects the other adapters, not this one.
- Flagged that `browser.py:51` currently tells users to install a `noveltrans[browser]`
  extra that does not exist yet; Step 8 makes that message true.
- **Sub-step 3a**: settle headed-vs-headless by experiment (run the capture script with
  `headless=True`) rather than by argument, before wiring the default. The challenge
  cleared passively for real Chrome, so headless has a genuine chance — and if it works
  the 199-navigation visible-window UX problem disappears.

## [STEP 3] Browser session — headless is fingerprinted — 2026-07-16
Plan 015.02 §3 Step 3. Added `src/noveltrans/cf_browser.py` + `tests/test_cf_browser.py`.

### Sub-step 3a VERDICT: headless is fingerprinted — headed default stands
Ran `scripts/capture_69shuba.py --headless` against the real site. It sat on the
challenge for the full **180s timeout and never cleared**, where headed clears
instantly. So Cloudflare fingerprints headless Chrome here, and the headed default is
now settled **on evidence rather than analogy** (015.02 §5 wanted exactly this).

Consequence: the visible-window UX imposition is unavoidable, not a preference. The
`headless` parameter stays exposed (a future CF policy change could make it viable)
but will very likely fail today; the module docstring records the measurement so the
next person doesn't re-run it.

### Sub-step 3b: `challenge.html` fixture — and a bug in the capture of it
First attempt saved a **14-byte 404** as the challenge fixture. Cause: the request
sent `User-Agent: python-requests`, and the site answers an obvious bot UA with a bare
404 rather than the Cloudflare interstitial. Sending `base.USER_AGENT` returns the real
thing: **403, `cf-mitigated: challenge`, 5,652 bytes**. Fixed, and `capture_challenge_
fixture()` now refuses to save anything that isn't an interstitial — a 14-byte fixture
would have made `looks_like_challenge` tests pass while proving nothing.

### `cf_browser.py`
- `BrowserSession` — one persistent context per scan/download batch, **not** a
  `solve()` returning a `Clearance`: there is no token, so the live session *is* the
  clearance (015.02 §2).
- **Lazy launch** — constructing a session never starts Chrome (adapters get built for
  cancelled scans and in tests).
- **One page, one `page.route()`** for the session's life; not re-paid per navigation.
- `_BLOCKED_TYPES = {image, font, media, stylesheet}` — never `document`/`script`/`xhr`,
  since CF's check is JS-driven. Kept a module constant so emptying it is the first
  diagnostic if 403s appear.
- `_throttle()` — copies `HttpClient._throttle`'s shape. **The only thing between a
  199-chapter batch and hammering the site**, since `HttpClient`'s throttle is bypassed
  entirely on this path.
- `looks_like_challenge(markup)` — pure, no Playwright import; folded in here rather
  than a `scrapers/cloudflare.py` module, per 015.02 §2 (one predicate, one caller).
- Navigation failure (closed window / crash) closes the session and raises, so a dead
  browser isn't retried 198 more times.
- Docstring records the measured facts and the do-not-parallelise warning, following
  `discord_unlock.py`'s candour precedent.

### Tests: 16 new, none launch a browser
`BrowserSession` takes `sync_playwright` injectably — Step 1's design paying off.
Covers: lazy launch, one-context-reuse across navigations, route registered once,
throttle between navigations, challenge→raise (against the **real** interstitial),
content→no false positive (against all three real content fixtures), navigation
failure closes the session, `close()` idempotent/pre-launch/context-manager, and
`.cf-profile` ≠ `.discord-profile`.

Self-review caught one junk test: `assert ".txtnav" in session.get_html(…) or True` —
vacuously true, and its second assertion tested the fixture rather than the code.
Rewritten to assert `get_html` returns the markup verbatim.

### Verified against the real site (not just fakes)
Drove a real `BrowserSession` through all three page types:
| page | time | chars | challenge |
|---|---|---|---|
| `/book/59024.htm` | 2.26s (incl. launch) | 12,999 | False |
| `/book/59024/` | 1.18s | 26,630 | False |
| `/txt/59024/38369377` | 1.58s | 29,863 | False |

One context reused for all three, closed cleanly, **no leaked Chrome processes**
(`pgrep` clean). Navigations ~1.2s with blocking on, so 199 chapters ≈ 5 min — the
1.5s politeness delay dominates, as 015.02 §3 predicted (the throttle measures from
request *start*, so effective delay is `max(delay, nav_time)`, matching `HttpClient`).

Verified: `272 passed, 5 deselected` (was 256); `ruff check src tests` clean.

## [STEP 4] Pure parse functions — 2026-07-16
Plan 015.02 §3 Step 4. Added `src/noveltrans/scrapers/shuba69.py` (parsing surface
only — the adapter class and registration are Step 5) and
`tests/test_scrapers_shuba69.py`.

Module name is `shuba69.py`, not `69shuba.py`: the latter can't be imported (invalid
identifier). `name = "69shuba"` remains the adapter id and fixture dir.

### What's in it
- **URL derivation** — `book_id` / `info_url` / `toc_url`. One book id yields *three*
  pages; metadata and the chapter list are NOT the same URL, which was 015.01's biggest
  structural error, so it gets its own tests. `_origin()` preserves the scheme+host the
  user pasted so mirrors keep working.
- **`parse_metadata`** — OpenGraph tags (`ixdzs`'s `og()` helper shape).
  `NovelMeta.url` echoes the caller's URL unchanged: `ScanWorker` keys projects off it
  via `library.find_by_url`, so rewriting it would orphan them.
- **`_clean_description`** — `og:description` carries *literal* `<br />` text as a meta
  attribute value, so BeautifulSoup never sees those tags and can't strip them. Unescape
  to newlines, collapse blank runs.
- **`parse_chapter_list`** — `#catalog ul li a[href]`, no pagination loop.
- **`parse_chapter`** — `.txtnav`, decompose chrome, `<br>`-split (the `xbanxia` shape).
  `SEL_CHROME` targets the five known chrome nodes by name rather than copying
  `xbanxia`'s blanket `span, div, script, ins`: `.txtnav` holds bare `<span>`s, and
  decomposing every `<div>` would silently eat content the day the site wraps a
  paragraph in one.
- **`_clean_title`** — strips a leading `N.` index prefix. Applied at TOC-parse time
  (so titles are uniform for the EPUB TOC and TTS, per feature 011's precedent) and to
  *both sides* of the title-dup comparison in `parse_chapter`, since titles already in
  the project DB predate the normaliser.

### The title-dup strip, and why it's conservative
Verified trap: TOC gives `1.第1章 …` while the body line is `第1章 …`, so `xbanxia`'s
`lines[0] == ref.title` would silently fail to strip on exactly the prefixed chapters.
Fixed by normalising both sides, then demanding **equality** — not `endswith`, which
015.01 floated. The failure modes are asymmetric: under-stripping leaves a duplicated
heading (cosmetic), over-stripping silently deletes a real opening line (data loss the
reader won't notice until later). `endswith` would happily eat a short line of dialogue.
So: remove one well-defined prefix, demand equality, never drop more than one line.
Tested in both directions, including "ref title doesn't match → nothing is stripped".

### Tests: 30 new, zero browser, zero mocks
Against the real fixtures. Covers URL derivation from either URL form, `_clean_title`
(prefix present/absent/fullwidth `．`/ideographic `、`, idempotence, and that it never
eats the `第N章` itself), OG metadata, description unescaping, 199 refs with prefixes
stripped, body extraction, chrome removal, and each `ScrapeError` path.

### Verified live, not just against fixtures
Drove a real `BrowserSession` through the parse functions end to end: title/author/
cover read, 199 chapters, prefixes stripped, ch.1 body parsed with the dup heading
stripped and chrome gone. Live output reconciles **exactly** with the fixture (2,269
chars = 2,102 fixture line-chars − 13 title chars + 90 paragraph separators × 2), so
there's no drift between capture and now, and `page.content()` under resource-blocking
parses identically to the unblocked capture.

Verified: `302 passed, 5 deselected` (was 272); `ruff check src tests scripts` clean.

## [STEP 5] Adapter wiring + registration — 2026-07-16
Plan 015.02 §3 Step 5. `Shuba69Adapter` added to `src/noveltrans/scrapers/shuba69.py`;
registered in `scrapers/__init__.py`'s `_import_adapters()`. The adapter is now
reachable from the app.

- `url_patterns = [r"69shuba\.(?:com|cx)/book/\d+"]` — matches both `/book/59024.htm`
  and `/book/59024/` since `matches()` uses `re.search`. Deliberately conservative:
  only `www.69shuba.com` has actually been read past the challenge; `.cx` is the same
  operator's known mirror (worst case there is a clean `ScrapeError`), and `69shu.com`
  is left out until verified. Avoids 015.01's proposed `r"69shuba?\."`, which parses as
  `69shub` + optional `a` — matching `69shub.com` and missing `69shu.com`.
- `_get_html` is the only browser-touching method, as designed since 015.01 §1. Builds
  the `BrowserSession` lazily and seeds `delay_seconds` from `client.delay_seconds` —
  `HttpClient`'s throttle is bypassed on this path, so the config'd politeness delay
  has to be carried across explicitly or a batch would hammer the site.
- Errors translated to Vietnamese at this boundary (`BrowserUnavailableError` → install
  Chrome/Playwright; `BrowserSessionError` → browser closed or challenge not cleared),
  mirroring `discord_unlock`'s pattern of keeping infrastructure English and translating
  at the user-facing edge.
- `close()` releases the session; idempotent, safe before any fetch. Nothing calls it
  yet — the `SiteAdapter.close()` default and the workers' `finally` teardown are Step 6.
- `headless` is exposed but defaults to `False`: headless is fingerprinted and does not
  clear the challenge (measured, Step 3a).

### Tests: 17 more (47 in this file, 319 total)
`TestRegistry` — both URL forms match; other sites' URLs don't; `adapter_for_url`
resolves here; **and a guard that registration doesn't shadow another adapter**
(`ixdzs8.com/read/…` still resolves to `IxdzsAdapter`).
`TestAdapterWiring` — `_session` replaced with a fake serving fixtures by URL, so no
browser launches. Asserts metadata hits `/book/<id>.htm` while the chapter list hits
`/book/<id>/` (**the structural correction 015.01 got wrong, so it gets its own test**),
that `NovelMeta.url` is echoed rather than rewritten to the `.htm` page, that either
pasted URL form resolves the same, one-session reuse, `close()` idempotence, that
constructing never launches, and that `delay_seconds` is inherited from the client.

Registry sanity: all five adapters (`giatocvuongtai`, `ixdzs`, `medoctruyen`,
`69shuba`, `xbanxia`) resolve to the right class for their own URLs.

### Verified live through the app's own entry point
`adapter_for_url(url, HttpClient(delay_seconds=1.5))` → `Shuba69Adapter`; metadata,
199 chapters, then two chapters fetched on one reused session at ~0.8s each; `close()`
clean, no leaked Chrome processes. The full path — registry → adapter → browser →
parse — works against the real site.

Verified: `319 passed, 5 deselected` (was 302); `ruff check src tests` clean.

## [STEP 6] Adapter teardown — 2026-07-16
Plan 015.02 §3 Step 6. `Shuba69Adapter.close()` existed after Step 5 but **nothing
called it**, so a real scan would have left an orphan Chrome window behind. Closed that.

- **`SiteAdapter.close()`** in `scrapers/base.py` — a documented no-op default. The four
  existing adapters fetch over the shared `HttpClient` and hold nothing, so they inherit
  it and are unaffected. This is the *only* `base.py` change this feature makes; all
  three of 015.01's proposed `HttpClient` changes stayed dropped.
- **`ScanWorker.run`** — `adapter = None` before the `try`, `finally: if adapter is not
  None: adapter.close()`. The `finally` runs after the existing `except` clauses, so the
  failure paths (which are exactly the ones that leak) are covered too.
- **`DownloadWorker.run`** — same, nested above the existing `finally: project.close()`.

Editing `DownloadWorker` needed care: its `run()` tail is byte-identical to
`TranslateWorker`'s, so an unanchored edit would have silently patched the wrong worker.
Anchored on `daily_limit_hit`, which only `DownloadWorker` has.

### Tests: 7 new
`tests/test_workers_adapter_close.py`, with a `_FakeAdapter` counting `close()` calls.
Covers `ScanWorker` on success **and on failure**, `DownloadWorker` after a normal batch,
after a chapter error, and after `cancel()` — plus `adapter_for_url` returning `None`
(nothing to close, and no `AttributeError`), and that the inherited no-op `close()` on a
holds-nothing adapter never raises.

### Verified live — the actual point of this step
Ran a **real `ScanWorker`** against 69shuba: 199 chapters scanned, project created, and
`pgrep` shows **0 Chrome processes before and 0 after**. The fake-adapter tests prove the
call happens; this proves the browser actually dies.

Verified: `326 passed, 5 deselected` (was 319); `ruff check src tests` clean.

## [STEP 7] Status plumbing — and the headless toggle deliberately NOT shipped
Plan 015.02 §3 Step 7. 2026-07-16.

### Shipped: the status message
A Chrome window appearing unannounced mid-scan reads as the app misbehaving, so the
adapter now says why *before* it opens.
- **`SiteAdapter.on_status`** (`Callable[[str], None] | None`) + a `_status()` helper in
  `base.py`. A class attribute assigned by workers, so `adapter_for_url`'s signature —
  which four adapters and their tests depend on — stays untouched. No-op outside the GUI.
- **`ScanWorker.progress = Signal(str)`** — it had *no* progress signal before, only
  `scanned`/`failed`, so a scan was entirely silent. Wired to `adapter.on_status` and
  connected to `tab_scrape`'s status label.
- **`DownloadWorker`** — `on_status` wired to its existing `progress(done, total, msg)`.
  The lambda closes over `run()`'s locals, so it reads `done`/`total` at call time: a
  mid-batch browser relaunch reports the real position rather than a stale 0.
- Message echoes the existing *"giữ cửa sổ mở"* phrasing from `tab_scrape.py`.

### NOT shipped: the headless Settings toggle — a deliberate deviation from the plan
015.02 §5 wanted a `shuba69_headless` config property and a Settings checkbox labelled
*"thử nghiệm"*, on the reasoning that CF policy might change. **Skipped**, because Step
3a *measured* headless as broken: it never clears the challenge (180s timeout vs instant
headed). Shipping a checkbox that is ~guaranteed to break the feature isn't an option,
it's a trap — the user ticks it, every fetch fails, and the cause is invisible. Settings
also carry a permanent support and maintenance cost.

The `headless` constructor parameter **stays** on `Shuba69Adapter`/`BrowserSession` so
the experiment is one keyword away and the default can be flipped in code if Cloudflare
ever changes. Nothing is lost but the footgun. Reversible in ~15 lines if wanted.

Consequence: no `config.py` or `settings_dialog.py` changes this step, and the workers
need no new kwarg to carry the flag.

### Tests: 3 new (10 in this file, 329 total)
`_status` is a no-op with no listener (adapters run outside the GUI in scripts/tests);
`ScanWorker` forwards adapter status to `progress`; `DownloadWorker` forwards it **with
the real batch position** rather than a placeholder.

### Verified live — the ordering is the whole point
Real `ScanWorker` against 69shuba:
```
 0.00s  STATUS   🌐 Đang mở trình duyệt để vượt kiểm tra Cloudflare của 69shuba — giữ cửa sổ mở…
 4.46s  SCANNED  <title> / 199 ch
```
The explanation lands at 0.00s, *before* the window opens; scan completes at 4.46s; no
leaked Chrome. A message that arrived after the window would have been pointless.

Verified: `329 passed, 5 deselected` (was 326); `ruff check src tests` clean.
