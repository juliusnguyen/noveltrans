# 018 — Adjustable TTS settings — History

## [FEATURE] 018-TTS-ADJUST — 2026-07-18
Created feature scaffolding and branch `changes/018-TTS-ADJUST`.
Prompt: `018.00-PROMPT.md`.

## [DECISIONS] Scope settled before planning — 2026-07-18
User chose all four adjustables, plus asked for a "steps/accuracy" control.

### Four confirmed knobs (verified to exist)
1. **Pause length** — `paragraph_gap_seconds` (0.4, hardcoded in `tts/base.py`).
2. **Speaking speed** — NOT native to VieNeu; needs ffmpeg `atempo` post-processing.
3. **Volume/gain** — numpy scalar multiply on the samples.
4. **Expressiveness (temperature)** — VieNeu `infer`'s `temperature` param.

### The "steps" knob does NOT exist — corrected with the user
The user remembered a "more steps = more accurate" control. Checked the installed
VieNeu source: this is the **`v3turbo`** build — an **autoregressive LM TTS** (LMDeploy
backbone, `temperature`/`top_k` sampling), **not diffusion**. There is no
NFE/steps/num_inference_steps parameter anywhere in `infer`. That control belongs to
diffusion-TTS (e.g. F5-TTS), which this isn't. Told the user plainly rather than build a
slider that does nothing. `top_k` is the nearest lever but it's not an accuracy dial —
recommended leaving it out.

### Verified pipeline facts
- `synthesize_chapter` (`tts/base.py`) is the single chokepoint (both AudioWorker paths
  + CLI). Pause + volume apply here; temperature threads into `vieneu.py::synthesize`'s
  `infer(...)`; speed is a post-process (ffmpeg atempo) like the mp3 conversion.
- `convert.py` already runs ffmpeg for mp3 — atempo belongs there. Constraints: atempo
  range 0.5–2.0 (chain beyond), speed changes duration (recompute), and WAV output would
  now need ffmpeg for speed (gate the control on `ffmpeg_available()`, as the mp3 option
  already is).
- Defaults must reproduce today's output (gap 0.4, speed 1.0, volume 1.0, temperature =
  pass-nothing for exact parity).

## [PLAN] 018.01-INITIAL-PLAN — 2026-07-18
Generated plan: `018.01-INITIAL-PLAN.md`. Four knobs: gap/volume/temperature at the
`synthesize_chapter` chokepoint, speed as an ffmpeg atempo post-process. Defaults
reproduce today's output. No "steps" knob (v3turbo is autoregressive, not diffusion).

## [STEP 1-2] Config + synthesize_chapter gap/volume — 2026-07-18
Plan 018.01 Steps 1-2. `config.py` and `tts/base.py`; tests in `tests/test_tts.py`.

- **`config.py`**: four `DEFAULT_TTS_*` constants (gap 0.4, speed 1.0, volume 1.0,
  temperature 0.0=unset) + clamped float properties (`tts_gap_seconds` 0.0–2.0,
  `tts_speed` 0.5–2.0, `tts_volume` 0.1–3.0, `tts_temperature` 0.0–1.5). Added a small
  `_clamp(v, lo, hi)` module helper; clamp applied in BOTH getter and setter so a value
  written directly to QSettings out of range still reads back clamped.
- **`tts/base.py::synthesize_chapter`**: added `gap_seconds: float | None = None`
  (None → the engine's `paragraph_gap_seconds`) and `volume: float = 1.0`. Volume is a
  final-buffer multiply hard-clipped to [-1, 1] (`np.clip`) so gains >1.0 can't wrap
  around into noise. Added a `gap.size` guard so `gap_seconds=0.0` inserts no empty pad.
  Both defaults reproduce today's output exactly.

### Tests: 11 new (416 total)
- `FakeTtsEngine.save_wav` now captures `last_samples`, so gain is assertable without a
  model. (1000 Hz, 1 sample/char → duration math is exact.)
- Gap: `gap_seconds=0.3` adds exactly +0.2s over the 0.1s default (two-chunk text);
  `gap_seconds=0.0` drops the pad (-0.1s). Volume: 0.5→0.5, default→1.0, 2.0→clipped 1.0.
- Config: defaults reproduce current behaviour, every knob clamps at both bounds,
  valid values round-trip.
- One test bug: first `_text()` fit in a single chunk so no gap was inserted; fixed to
  two >30-char sentences forcing two chunks (mirrors `test_chunks_and_duration`).

Verified: `416 passed, 5 deselected` (was 409); `ruff check` clean.
