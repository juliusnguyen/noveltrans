## [PLAN] 079.01-WINDOWS-TRANSLATE-NO-CONSOLE-PLAN — 2026-09-03
Generated plan: `079.01-WINDOWS-TRANSLATE-NO-CONSOLE-PLAN.md`

## [IMPLEMENT] 2026-09-03
Applied the plan's fix, reusing `no_console_kwargs()` from `runtime_env.py` (added in
052-TAG-LIMIT-AND-FFMPEG-CONSOLE, which had explicitly flagged this gap):

- `src/noveltrans/translators/cli_agent.py`: imported `no_console_kwargs` and spread it
  into the `subprocess.run(...)` call in `CliAgentTranslator.complete()` — this is the
  reported bug (console flash on every translated chapter/segment).
- `src/noveltrans/gui/workers.py`: same fix for `CliModelsWorker.run()`'s
  `subprocess.run(...)` call (spawns `<binary> models` to refresh the Settings model
  dropdown) — same defect class, found during the audit, not previously covered by 052
  since it isn't an ffmpeg call site.
- Audited every other `subprocess`/`Popen` call site in `src/noveltrans/` per the plan;
  all others (`tts/convert.py`, `tts/subtitles.py`, `tts/video.py`, `tts/merge.py`,
  `gui/keep_awake.py`'s macOS-only branch) already had the guard or don't spawn a
  process on Windows — no changes needed there.
- Tests: added `TestCliAgent.test_passes_no_console_kwargs_on_windows` and
  `test_no_console_kwargs_empty_on_other_platforms` to `tests/test_translators.py`, and
  a new `tests/test_workers_cli_models.py` with the equivalent pair for
  `CliModelsWorker`, all monkeypatching `runtime_env.sys.platform` to match the existing
  `TestNoConsoleKwargs` pattern in `tests/test_runtime_env.py`.

Verified: `pytest -q tests/test_translators.py tests/test_workers_cli_models.py
tests/test_runtime_env.py` — 67 passed. `ruff check` on every changed/new file shows no
findings introduced by this diff (compared line-for-line against the pre-change files
via `git stash`; the same pre-existing BLE001/S110/PLW1510/UP037/B023/I001/C408/RUF059
findings elsewhere in `workers.py` and `test_translators.py` were already present
before this change). Console-window suppression itself is OS chrome and not
unit-testable — needs manual verification on a Windows build (translate a chapter via
a CLI engine, confirm no console flash).
