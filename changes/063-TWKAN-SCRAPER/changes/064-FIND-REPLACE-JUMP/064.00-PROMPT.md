# 064 — Find & Replace: jump to the match

Two requests, shipped together on this branch.

## 1. README — `Trang web được hỗ trợ`

> Update `Trang web được hỗ trợ` in README.md

…which turned out to be accurate already (all 10 registered adapters listed, every URL
pattern matching `url_patterns`), so the work was closing the gaps around it. Followed by:

> add the video tab row too

## 2. Tìm & thay thế

> in the `Tìm & thay thế` [screenshot of the dialog, 63 matches in 63 chapters]
> when double click in each chapter, it should jump in to the found text so we could fix
> that text or sentence in the chapter.

The dialog previews per-chapter match counts but the list is inert: the only thing the
user can do with 63 rows is replace all 63 or close. Some matches want a hand edit of the
sentence around them instead.
