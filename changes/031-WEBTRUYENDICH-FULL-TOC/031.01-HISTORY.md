# 031 — webtruyendich full TOC — History

## [FEATURE] 031-WEBTRUYENDICH-FULL-TOC — 2026-07-24
Branch `changes/031-WEBTRUYENDICH-FULL-TOC`. Prompt: `031.00-PROMPT.md`.
Bug: scanning a webtruyendich novel missed most chapters (only ~135 of ~1700+).

## [INVESTIGATION] Live TOC — 2026-07-24
Confirmed on `/danh-sach-chuong-day-du` in a real browser: the chapter list is
**infinite-scroll / lazy-loaded**. Initial DOM ~135 links (max #149); scrolling
to the bottom repeatedly grew it to ~1692 and then stable (no growth over 3
checks). The 029 scraper did `goto → read HTML` with no scrolling, so it only saw
the first batch. The 029 test fixture had been captured after a `sleep(2)` that
loaded a fuller snapshot (1722), which hid the bug.

## [FIX] — 2026-07-24
- `webtruyendich_browser.py`:
  - `get_html(url, *, scroll_item_selector=None)` — with a selector, scroll to the
    bottom until that selector's element count stops growing (`_scroll_until_stable`:
    600 ms settle, stop after 3 stable rounds, 300-round cap), then read the HTML.
  - Made `_content_after_challenge` resilient: `page.content()` can throw
    "page is navigating" mid client-side redirect (the TOC does this). Now it
    retries content reads within the deadline instead of killing the session
    (this was the first live failure after adding scroll mode).
- `scrapers/webtruyendich.py` — `fetch_chapter_list` fetches the TOC with
  `scroll_item_selector=SEL_TOC_LINKS`; `_get_html` forwards the kwarg.
  `parse_chapter_list` unchanged.
- Tests — `_FakeSession.get_html` accepts the kwarg and records it; new test
  asserts the TOC is fetched in scroll mode while metadata is not. Browser
  scrolling itself isn't unit-tested (same as the rest of the browser path).

## [VERIFY] Live — 2026-07-24
`fetch_chapter_list` through the real adapter now returns **1725 chapters**
(was ~135): #1…#1764, ascending, 0 blank titles, ~10 s for the scroll-load.
Full suite **664 passed**; ruff clean.
