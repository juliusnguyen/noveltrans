# 051 — Support upload into OneDrive

> support upload into onedrive

## Open questions (to resolve during `/plan`)

* **What gets uploaded** — the rendered video, the audio, the subtitle sidecar, the
  translated chapters, or the whole library folder?
* **Where it fits** — a new destination alongside the existing YouTube upload path, or a
  separate action?
* **Auth** — Microsoft Graph OAuth (device code vs. redirect), where the token is stored,
  and how refresh is handled.
* **Target folder** — fixed path, remembered per job, or picked like the YouTube playlist
  picker (039) / library folder history (045).
